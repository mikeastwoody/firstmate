=== Regression check: the 9 new base-freshness tests, each run individually ===

--- against the PRE-CHANGE bin/fm-brief.sh (base commit 0866a77): all fail ---
not ok - ship brief did not number worktree isolation as the first safety step (missing: '1. **Verify isolation before anything else.**')
not ok - the runtime fallback lost its numbered place in the ship safety contract (missing: '2. **Check your base before starting work.**')
not ok - the stranded clone did not fall back to its local default-branch ref (missing: 'git rev-list --count $(git merge-base HEAD main)..main')
not ok - a non-root project directory did not fall back to the runtime base check (missing: 'git rev-list --count $(git merge-base HEAD origin/<default>)..origin/<default>')
not ok - an absent local ref for origin's default did not fall back to the runtime base check (missing: 'git rev-list --count $(git merge-base HEAD <default>)..<default>')
not ok - the local-only fixture no longer resolves its non-main default branch (missing: 'rebase onto `release`')
not ok - the scout local fallback did not measure against the local default branch (missing: 'git rev-list --count $(git merge-base HEAD main)..main')
not ok - the scout fallback dropped the single fetch that precedes its origin comparison (missing: 'git fetch origin --quiet')
not ok - the resolved scout path stopped degrading to the local default branch (missing: 'git rev-list --count $(git merge-base HEAD develop)..develop')

--- against the change under test (98ed9a4): all pass ---
ok - fm-brief.sh: startup base freshness is resolved, zero-only, and mode-aware
ok - fm-brief.sh: an unresolvable repo label still mandates the base check at runtime
ok - fm-brief.sh: a missing origin/HEAD never relabels the current feature branch
ok - fm-brief.sh: a directory inside a work tree never resolves as that project's repo
ok - fm-brief.sh: an absent local ref for origin's default never substitutes another branch
ok - fm-brief.sh: local-only Rule 1 names the resolved local default branch
ok - fm-brief.sh: a scout local fallback explains itself without claiming it lands locally
ok - fm-brief.sh: an unresolved scout fallback degrades to a local default before blocking
ok - fm-brief.sh: the scout fallback fetches before verifying and degrades like its resolved path

=== Hosted-runner simulation: no global/system git identity, user.useConfigOnly=true ===
-- sanity: a bare fixture commit aborts in this env --
Author identity unknown

-- the colocated suite under that same env --
FM_TEST_SUMMARY total=1 failed=0 skipped_gate=0 duration_ms=6215
